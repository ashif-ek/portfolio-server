{
  "name": "portfolio-server",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "server": "json-server --watch db.json --port 5000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  },
  "engines": {
    "node": "22.x"
  }
}
