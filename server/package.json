{
  "name": "json-server-app",
  "version": "1.0.0",
  "main": "server.js",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "json-server --watch db.json --port 5000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}

